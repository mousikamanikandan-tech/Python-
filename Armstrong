num = int(input("Enter a number: "))
temp = num
power = len(str(num))
sum_val = 0

while temp > 0:
    digit = temp % 10
    sum_val += digit ** power
    temp //= 10

if sum_val == num:
    print("Armstrong number")
else:
    print("Not Armstrong")
